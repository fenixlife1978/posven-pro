'use server';
/**
 * @fileOverview Este archivo ha sido limpiado. 
 * Puedes definir aquí tu nuevo flujo de IA.
 */

import {ai} from '@/ai/genkit';
import {z} from 'genkit';

// Ejemplo de nuevo flujo vacío
export async function newProjectFlow(input: any) {
  return { message: "IA lista para el nuevo proyecto" };
}
